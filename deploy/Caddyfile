:80 {
    log
    # Caddy runs as its own werkbank container, so the upstreams are on the host, not on
    # localhost. The path prefix is passed through: the Ktor routes live under /api.
    reverse_proxy /api* host.docker.internal:30137
    reverse_proxy * host.docker.internal:30138
}
